import type { Tile } from "../core";

// Draws 4 colored triangles + digits (colors via DIGIT_COLORS in theme.ts).
export function drawTile(
  _ctx: CanvasRenderingContext2D,
  _tile: Tile,
  _x: number,
  _y: number,
  _size: number,
): void {
  throw new Error("not implemented");
}
