import type { SolvedPuzzle } from "../core";

// Build a consistent solved layout, then shuffle its tiles into the pool.
// Returns the answer key too (SolvedPuzzle); strip to a plain Puzzle before
// handing it to a solver.
export function generate(n: number, seed?: number): SolvedPuzzle {
  throw new Error("not implemented");
}
